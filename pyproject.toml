[project]
name = "news-portal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.6",
    "psycopg2-binary>=2.9.12",
    "python-dotenv>=1.2.2",
]
